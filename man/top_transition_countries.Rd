% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/top_transition_countries.R
\name{top_transition_countries}
\alias{top_transition_countries}
\title{Find Countries with the Most Democratic Transitions}
\usage{
top_transition_countries(data, start_year = 1950, end_year = 2020, n = 10)
}
\arguments{
\item{data}{A data frame containing democracy and leadership data, such as
the output of \code{\link[=load_data]{load_data()}}. Must contain \code{country_name}, \code{year}, and
\code{is_democracy}.}

\item{start_year}{Numeric. Earliest year to include. Defaults to \code{1950}.}

\item{end_year}{Numeric. Latest year to include; must be at least
\code{start_year}. Defaults to \code{2020}.}

\item{n}{Integer. Number of top countries to return. Defaults to \code{10}.}
}
\value{
A tibble with one row per country (up to \code{n} rows) and columns:
\describe{
\item{\code{country_name}}{Country name.}
\item{\code{transitions_in}}{Number of years in which the country became a
democracy.}
\item{\code{transitions_out}}{Number of years in which the country ceased to
be a democracy.}
\item{\code{total_transitions}}{Sum of \code{transitions_in} and
\code{transitions_out}.}
}
Rows are sorted descending by \code{total_transitions}.
}
\description{
Filters the dataset to a user-specified year range and returns the top \code{n}
countries ranked by total number of democratic-regime transitions (both
entries into and exits from democracy).
}
\details{
Transitions are computed from the \code{is_democracy} column using a within-country
lag (\code{\link[data.table:shift]{data.table::shift()}}), so no pre-processing with \code{\link[=clean_data]{clean_data()}} is
required. The grouped lag and aggregation are performed with
\link[data.table:data.table-package]{data.table}, which keeps the function fast
on moderately large country-year panels.
}
\examples{
\dontrun{
dem <- load_data()
top_transition_countries(dem, start_year = 1970, end_year = 2010, n = 5)
}
}
