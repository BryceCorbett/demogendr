% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise_leader_gender_gap.R
\name{summarise_leader_gender_gap}
\alias{summarise_leader_gender_gap}
\title{Summarize Gender Gaps in Political Leadership}
\usage{
summarise_leader_gender_gap(data, group_var = "decade", leader = "president")
}
\arguments{
\item{data}{A data frame containing democracy and leadership data, such as
the output of \code{\link[=load_data]{load_data()}}, or a pre-processed tibble already containing
\verb{<leader>_gender} and \verb{<leader>_accession_age} columns.}

\item{group_var}{A character string giving the variable to group by, such as
\code{"decade"}, \code{"year"}, \code{"is_democracy"}, or \code{"region"}. Defaults to
\code{"decade"}.}

\item{leader}{A character string indicating which type of leader to
summarize. Use \code{"president"} for presidents or \code{"monarch"} for monarchs.
Defaults to \code{"president"}.}
}
\value{
A tidy tibble grouped by \code{group_var}, with summary columns for the
proportion of female leaders, the proportion of male leaders, the mean
accession age for female leaders, and the mean accession age for male
leaders.
}
\description{
Creates a tidy summary table comparing female and male leaders by a selected
grouping variable. The summary includes the proportion of leaders of each
gender and the mean accession age for each gender.
}
\details{
If the data does not already contain \verb{<leader>_gender} and
\verb{<leader>_accession_age} columns, the function will derive them from the raw
columns shipped in \code{\link[=load_data]{load_data()}} (\verb{is_female_<leader>}, accession year, and
birth year). If \code{group_var = "decade"} and no \code{decade} column is present, it
will be derived from \code{year}.
}
\examples{
\dontrun{
dem <- load_data()
summarise_leader_gender_gap(dem, group_var = "decade", leader = "president")
summarise_leader_gender_gap(dem, group_var = "is_democracy", leader = "monarch")
}
}
