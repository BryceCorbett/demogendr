% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_data.R
\name{clean_data}
\alias{clean_data}
\title{Clean the Democracy Dataset}
\usage{
clean_data(data)
}
\arguments{
\item{data}{A data frame such as the output of \code{\link[=load_data]{load_data()}}. Must contain
the columns \code{president_accesion_year}, \code{president_birthyear},
\code{monarch_accession_year}, \code{monarch_birthyear}, \code{is_female_president},
\code{is_female_monarch}, \code{year}, \code{country}, and \code{is_democracy}.}
}
\value{
A tibble with all original columns plus:
\describe{
\item{\code{president_age_at_accession}}{Age (years) when the president took
office.}
\item{\code{monarch_age_at_accession}}{Age (years) when the monarch took the
throne.}
\item{\code{has_female_leader}}{\code{1} if the country had a female president
\emph{or} monarch that year, \code{0} otherwise.}
\item{\code{decade}}{Character label such as \code{"1990s"} derived from \code{year}.}
\item{\code{to_democracy}}{\code{1} in years when a country transitioned \emph{into}
democracy, \code{0} otherwise.}
\item{\code{from_democracy}}{\code{1} in years when a country transitioned \emph{out
of} democracy, \code{0} otherwise.}
}
}
\description{
Adds derived columns to the raw dataset returned by \code{\link[=load_data]{load_data()}}:
age-at-accession for presidents and monarchs, a binary \code{has_female_leader}
indicator, a \code{decade} label, and year-over-year democratic-transition flags
(\code{to_democracy} / \code{from_democracy}).
}
\details{
The transition flags are computed per country using \code{\link[dplyr:lag]{dplyr::lag()}}: a
\code{to_democracy} event is recorded in the year a country's \code{is_democracy}
status flips from \code{FALSE} to \code{TRUE}, and vice-versa for \code{from_democracy}.
}
\examples{
\dontrun{
raw  <- load_data()
clean <- clean_data(raw)
head(clean[, c("country_name", "year", "decade",
               "to_democracy", "from_democracy",
               "president_age_at_accession")])
}
}
