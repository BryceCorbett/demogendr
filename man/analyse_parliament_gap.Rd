% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_parliament_gap.R
\name{analyse_parliament_gap}
\alias{analyse_parliament_gap}
\title{Test for a Gender-Representation Gap in Parliament}
\usage{
analyse_parliament_gap(data, group_col = is_democracy)
}
\arguments{
\item{data}{A data frame containing democracy and leadership data, such as
the output of \code{\link[=load_data]{load_data()}}. Must contain \code{prop_in_par} and the column
named by \code{group_col}.}

\item{group_col}{\<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}\> An unquoted
column name whose values define the two groups. Defaults to
\code{is_democracy}. The column must have exactly two distinct non-\code{NA} levels
for the t-test to be well-defined.}
}
\value{
A one-row \link[tibble:tibble-package]{tibble} produced by
\code{\link[broom:tidy]{broom::tidy()}}, with columns including \code{estimate1}, \code{estimate2},
\code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}, \code{parameter}, and
\code{method}.
}
\description{
Runs a Welch two-sample t-test comparing the proportion of parliamentary
seats held by women (\code{prop_in_par}) across two groups defined by a
user-supplied grouping column. The column is passed as an \strong{unquoted name}
using tidy evaluation, so no quotes are needed at the call site.
}
\examples{
\dontrun{
dem <- load_data()

# Compare women in parliament between democracies and non-democracies
analyse_parliament_gap(dem)

# Use a different grouping column (must be binary / two-level)
analyse_parliament_gap(dem, group_col = is_presidential)
}
}
