% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_regime_types2.R
\name{compare_regime_types2}
\alias{compare_regime_types2}
\title{Compare a Numeric Variable Between Two Political Regime Types}
\usage{
compare_regime_types2(data, outcome_var, regime_var, regime1, regime2)
}
\arguments{
\item{data}{A data frame containing the variables named by \code{outcome_var} and
\code{regime_var}.}

\item{outcome_var}{A character string naming the numeric column to compare
(e.g. \code{"prop_in_par"}).}

\item{regime_var}{A character string naming the categorical column used to
define the two groups (e.g. \code{"regime_category"}).}

\item{regime1}{A character string for the first regime value
(e.g. \code{"Democracy"}).}

\item{regime2}{A character string for the second regime value
(e.g. \code{"Non-Democracy"}).}
}
\value{
A one-row \link[tibble:tibble-package]{tibble} produced by
\code{\link[broom:tidy]{broom::tidy()}}, with columns including \code{estimate1}, \code{estimate2},
\code{statistic}, \code{p.value}, \code{conf.low}, \code{conf.high}, \code{parameter}, and
\code{method}.
}
\description{
Performs a Welch two-sample t-test comparing the mean of a numeric variable
between two groups defined by a categorical regime column. Rows with missing
values in \code{outcome_var} are dropped with a warning before the test is run.
}
\examples{
\dontrun{
dem <- load_data()

# Compare women in parliament between TRUE/FALSE democracy status
compare_regime_types2(dem,
  outcome_var = "prop_in_par",
  regime_var  = "is_democracy",
  regime1     = TRUE,
  regime2     = FALSE)
}
}
